# Render seteaz? PORT; local r?mâne 8080
server.port=${PORT:8080}

# Ia DB din env pe Render; local folose?te fallback
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/parking_db}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:postgres}

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
spring.jpa.show-sql=true

# JWT (secret din env Ã®n production)
app.jwt.secret=${APP_JWT_SECRET:1234567890ABCDEF1234567890ABCDEF1234567890ABCDEF1234567890ABCDEF}
app.jwt.expirationMinutes=${APP_JWT_EXP_MINUTES:10080}

spring.cache.type=simple