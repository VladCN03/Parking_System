# Server (Render seteaz? PORT)
server.port=${PORT:8080}

# PostgreSQL (ia din env pe Render / Docker; altfel folose?te local)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/backend_db}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:postgres}

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
spring.jpa.show-sql=true

# JWT (secret din env în production)
app.jwt.secret=${APP_JWT_SECRET:1234567890ABCDEF1234567890ABCDEF1234567890ABCDEF1234567890ABCDEF}
app.jwt.expirationMinutes=${APP_JWT_EXP_MINUTES:10080}

spring.cache.type=simple